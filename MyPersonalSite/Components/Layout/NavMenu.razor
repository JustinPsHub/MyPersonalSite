@* No @rendermode here — inherits from the page, and layout is static *@

<aside class="app-sidebar" role="navigation" aria-label="Main">
    <div class="sidebar-header d-flex justify-content-between align-items-center mb-2">
        <a class="text-decoration-none text-white fw-bold" href="/">MyPersonalSite</a>
        <label for="menuToggle"
               class="btn btn-sm btn-outline-light d-lg-none mb-0"
               aria-label="Close menu">✕</label>
    </div>

    <nav class="nav flex-column gap-1">
        <NavLink href="/" Match="NavLinkMatch.All" class="nav-link">Home</NavLink>
        <NavLink href="/resume" class="nav-link">Resume</NavLink>
        <NavLink href="/visuals" class="nav-link">Visuals</NavLink>
        <NavLink href="/about" class="nav-link">About</NavLink>
        @* Remove these if not needed *@
        @* <NavLink href="/counter" class="nav-link d-none d-lg-block">Counter</NavLink>
        <NavLink href="/weather" class="nav-link d-none d-lg-block">Weather</NavLink> *@
    </nav>

    <div class="mt-auto small opacity-75 d-none d-lg-block">
        © @DateTime.UtcNow.Year • Justin
    </div>
</aside>
@code {
    public void Toggle() => StateHasChanged();
}   