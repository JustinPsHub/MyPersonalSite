@* @inherits LayoutComponentBase

<div class="page">
    <div class="sidebar">
        <NavMenu />
    </div>

    <main>
        <div class="top-row px-4">
            <a href="https://learn.microsoft.com/aspnet/core/" target="_blank">About</a>
        </div>

        <article class="content px-4">
            @Body
        </article>
    </main>
</div>

<div id="blazor-error-ui" data-nosnippet>
    An unhandled error has occurred.
    <a href="." class="reload">Reload</a>
    <span class="dismiss">🗙</span>
</div>
 *@

@inherits LayoutComponentBase

@* <div class="app d-flex">
    <!-- Left sidebar (desktop) / Drawer (mobile) -->
    <NavMenu @ref="_nav" />

    <!-- Main content -->
    <main class="flex-fill">
        <!-- Mobile top bar with hamburger -->
        <div class="topbar d-lg-none d-flex justify-content-between align-items-center">
            <button class="btn btn-outline-primary btn-sm" @onclick="ToggleMenu">☰ Menu</button>
            <a class="text-decoration-none fw-semibold" href="https://learn.microsoft.com/aspnet/core/" target="_blank" rel="noopener">About</a>
        </div>

        <!-- Page content -->
        <div class="page p-3">
            @Body
        </div>
    </main>
</div> *@

<div class="app">
    <div class="nav-host">
        <NavMenu @ref="_nav" />
    </div>

    <main class="flex-fill">
    @*     <div class="topbar d-lg-none d-flex justify-content-between align-items-center">
            <button class="btn btn-outline-primary btn-sm" @onclick="ToggleMenu">☰ Menu</button>
            <a class="text-decoration-none fw-semibold" href="https://learn.microsoft.com/aspnet/core/" target="_blank" rel="noopener">About</a>
        </div> *@

        <div class="page">
            @Body
        </div>
    </main>
</div>


<!-- Standard Blazor error UI -->
<div id="blazor-error-ui" data-nosnippet>
    An unhandled error has occurred.
    <a href="." class="reload">Reload</a>
    <span class="dismiss">🗙</span>
</div>

@code {
    private NavMenu? _nav;
    private void ToggleMenu() => _nav?.Toggle();
}

